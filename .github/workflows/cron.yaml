name: Nightly Cleanup
on:
  push:
    branches:
    - main
 # schedule:
    #- cron: '00 12 */5 * *'
   # - cron: '*/30 * * * *'

env:
  GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
  ENABLED: 'true'
jobs:
  clean:
    name: Clean nightly releases
    runs-on: ubuntu-latest
    strategy:
      matrix:
        repo: ["cron"]
    steps:
      - name: Logs
        run: |
          echo "ok"
      - name: Releases
        run: |
          if [ $ENABLED == 'true' ]; then \
            gh api --paginate repos/dragonhunt02/${{ matrix.repo }}/releases | \
            jq -r '.[] | [.id, .tag_name] | @tsv' > test.txt && cat test.txt #\
            while read -r id value; do
              echo "Deleting release id: $id value: $value"
              # gh api -X DELETE "repos/dragonhunt02/${{ matrix.repo }}/releases/$id"
            done;
          fi
